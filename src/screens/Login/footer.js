import { View, Text } from "react-native";
import React from "react";

const FooterComponent = ({ pStyle }) => {
   return (
      <View style={pStyle}>
         <Text>FooterComponent</Text>
      </View>
   );
};

export default FooterComponent;
